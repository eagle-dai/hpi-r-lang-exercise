codeoceanR::rt_score()

# T1 ----
# Read both Apples files and combine them below each other.
apples <- 0


# T2 ----
# Read the Age file and connect both records. 
# Only complete records are needed. 
# Place the arguments for the dataframes so that the resulting
# column order is Age, Apples (not reversed).
appleAge <- 0


# T3 ----
# Join the age data with the apple data again.
# This time, keep all entries from Age.
# Apple owners with unknown age are not needed.
appleAge_ageComplete <- 0


# T4 ----
# For visual understanding of 'merge' the following web pages are helpful:
if(FALSE) # do not run the next line when script is run:
browseURL("https://www.datasciencemadesimple.com/join-in-r-merge-in-r/")
if(FALSE)
browseURL(paste0("https://web.archive.org/web/20211119033409/",
                 "https://www.dummies.com/programming/r/how-to-use-the-merge-function-with-data-sets-in-r/"))
# Which joining method does 'merge' with 'all=TRUE' correspond to?
# 1. self join
# 2. inner join
# 3. outer join
# 4. cross join
rightJoin <- 0


# If you want to see your score on openHPI, submit it with:
# codeoceanR::rt_submit()
