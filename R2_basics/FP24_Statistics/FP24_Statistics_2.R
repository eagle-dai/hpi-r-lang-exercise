codeoceanR::rt_score()

# Two vectors with a selection from the built-in dataset 'trees':
height   <- c( 70,  65,  63,   72,   81,   83, 66, 75,   80  , 75,   79,   76,   76)
diameter <- c(8.3, 8.6, 8.8, 10.5, 10.7, 10.8, 11, 11, 11.1, 11.2, 11.3, 11.4, 11.4)

# T4 ----
# Sort both vectors in ascending order of the first ('height').
# 'height' and 'diameter' themselves should not be changed.
# Hint: see slide "Sorting" (Nr 53)
# Attention: do not use the sorted version to determine the height order.
height_sorted <- 0
diameter_sorted <- 0


# Now continue in "FP24_Statistics_3.R"
